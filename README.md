# postgres-custom-docker

Extension of the official postgres docker image, automatically including any *.conf file.

## build image

To add this image in your local docker repository, run:

```shell
docker build -t postgres-custom .
```
